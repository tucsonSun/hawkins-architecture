import { EgrStickyMenuDirective } from './egr-sticky-menu.directive';

describe('EgrStickyMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new EgrStickyMenuDirective();
    expect(directive).toBeTruthy();
  });
});
