import { EgrScrollToDirective } from './egr-scroll-to.directive';

describe('EgrScrollToDirective', () => {
  it('should create an instance', () => {
    const directive = new EgrScrollToDirective();
    expect(directive).toBeTruthy();
  });
});
